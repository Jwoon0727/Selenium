이기능은 /apart 경로에 새로 페이지를 만들어서 구현할거야 
/dashboard 페이지에서 버튼을 만들어서 여기 apart로 넘어가는 버튼을 만들어줘 p
내가생각한 플랜은 

일단 직접 

https://fin.land.naver.com/map?center=3zhF48-2AM354&zoom=15&layer=NobwRAlgJmBcZQKYGMA2BDAToqB9ADphMomADRj5boC2AznOMgPY36qIAeAkjPAEwA2QQA4AnGAC%2BFOoizIAFgAVq9RmABGAVwiooEAHYBzOGEHlKAT0TNjAFUv5S8ACxTJAXSA


여기 url들어가서 동마다 호수데이터를 캡쳐해 그리고 
제미나이한테 json형태로 뽑아달라고 해 
그걸 복사해서 

프로그램 iunput에 무슨동인지 / 그리고 json데이터를 넣는거야
이걸 여러번 보통 10개정도 
이작업을 반복해 
그리고 나서 출력하기 버튼을 누르면 
내가 원하는 엑셀데이터 파일이 나오는거지 
백석푸르지오아파트 엑셀데이터 파일 한개가 나오는 거야 

여기서 일단 db는 로컬스토리지를 사용할거야


---

# 📐 구현 계획 (`/apart` 페이지)

## 1. 목표
네이버 부동산 지도에서 동별 호수 데이터를 캡쳐 → Gemini로 JSON 변환 →
프로그램에 **`동 이름 + JSON`** 을 반복 입력(약 10개 동) →
**"출력하기"** 클릭 시 아파트 단지 1개의 엑셀 파일 생성. DB는 **로컬스토리지** 사용.

## 2. 기존 자산 재사용 포인트
- **엑셀 출력 패턴**: `app/dashboard/page.tsx:137` 의 `import('xlsx')` → `json_to_sheet` → `writeFile` 흐름 차용.
- **로컬스토리지 패턴**: `downloadHistory` 저장/로드 방식(`page.tsx:107-125`) 재사용.
- **UI 컴포넌트**: `components/ui/*` (button, card, dialog, input, label, table), `toast`(`app/hooks/use-toast`).
- 지도/AddressTable 관련 코드는 이 페이지에서 **불필요** (지도 캡쳐는 외부에서 수동 수행).

## 3. 신규/수정 파일

| 파일 | 작업 | 내용 |
|------|------|------|
| `app/apart/page.tsx` | 신규 | apart 메인 페이지 (동 입력·목록·엑셀 출력) |
| `app/dashboard/page.tsx` | 수정 | 헤더에 "아파트 입력" 버튼 추가 → `/apart` 이동 (`next/link` 또는 `useRouter`) |
| `components/apart/ApartMetaForm.tsx` | 신규(선택) | 단지 공통 입력(아파트명 · 도로명 · 번지) |
| `components/apart/DongInputForm.tsx` | 신규(선택) | 동 이름 + JSON(호수 배열) 일괄 입력 폼 (파싱·검증, 최대 ~10개) |
| `components/apart/DongList.tsx` | 신규(선택) | 입력된 동 목록 표시/삭제/편집 |
| `lib/apartStorage.ts` | 신규(선택) | 로컬스토리지 read/write 유틸 (키: `apart:draft`) |
| `lib/apartExcel.ts` | 신규(선택) | 호수 배열 → 6컬럼 조립 → 엑셀 변환/다운로드 로직 |

## 4. 데이터 흐름 & 상태 설계
```
[단지 공통 입력]  아파트명 · 도로명 · 번지
        +
[동 일괄 입력 폼]  101동 / JSON,  102동 / JSON, ... (최대 ~10개)
        │  ──추가──▶  ApartDraft  ◀──자동저장/복원── [로컬스토리지: apart:draft]
        │
   [동 목록 UI] (편집/삭제/순서)
        │
   [출력하기] ──▶ 전체 호수 flatten ──▶ xlsx 1개 파일
```
```ts
ApartDraft = {
  apartName: string,        // 아파트명 (예: 백석푸르지오)
  roadName: string,         // 도로명
  lotNumber: string,        // 번지
  dongEntries: DongEntry[]  // 최대 ~10개
}
DongEntry = { id: string, dongName: string, units: string[] }
//  units: 호수 문자열의 평면 배열 (예: ["2101","2102",...])
```
- **아파트명 · 도로명 · 번지**는 단지 공통 값(한 번 입력).
- **동 이름 + JSON**은 여러 동을 일괄 입력(최대 ~10개).
- 페이지 진입 시 `apart:draft` 복원, 변경 시마다 저장(초안 유지 → 새로고침에도 데이터 보존).
- "출력하기" 후 초안 비우기 여부는 옵션(확인 dialog).

## 5. 핵심 로직
1. **단지 공통 입력**: 아파트명 · 도로명 · 번지 각 1개 input.
2. **동 일괄 입력**: `동 이름 + JSON(호수 배열)` 쌍을 여러 개(최대 ~10개) 입력. "동 추가" 버튼으로 행 추가.
3. **JSON 파싱**: `JSON.parse` + try/catch. 형태는 **호수 문자열의 평면 배열** (`["2101","2102",...]`). 실패 시 toast 에러.
4. **엑셀 출력(조립)**: 모든 동의 각 호수를 1행으로 flatten → `/dashboard`와 **동일한 고정 엑셀 형식**으로 1개 파일 생성.
5. **파일명**: 아파트명 기반 default(예: `백석푸르지오.xlsx`), 편집 가능.

## 6. ✅ 확정된 스펙 (사용자 추가사항 반영)

### 6-1. 입력 JSON 형태 (동 하나당) + 일괄 입력
호수 문자열의 **평면 배열**:
```json
[
  "2101", "2102", "2001", "2002", "1901", "1902", "1801", "1802",
  "1701", "1702", "1601", "1602", "1501", "1502", "1401", "1402",
  "1301", "1302", "1201", "1202", "1101", "1102", "1001", "1002",
  "901", "902", "801", "802", "701", "702", "601", "602",
  "501", "502", "401", "402", "301", "302", "201", "202",
  "101", "102"
]
```
- **여러 동을 일괄 입력** — `동 이름 + JSON` 쌍을 최대 ~10개까지:
  ```
  101동 / [ ...json... ]
  102동 / [ ...json... ]
  103동 / [ ...json... ]
  ```
- 단지 공통값 **아파트명 · 도로명 · 번지**는 별도 input(한 번 입력).
- 최종적으로 이 모든 동을 **하나의 엑셀 파일**로 합침.

### 6-2. 엑셀 출력 형식 = `/dashboard`와 동일 (고정)
`app/dashboard/page.tsx:143-150` 기준, 컬럼 6개 고정:

| 도로명 | 번지 | 건물명 | 새부주소 | 금지 | 메모 |
|--------|------|--------|----------|------|------|

- 헤더 순서: `['도로명', '번지', '건물명', '새부주소', '금지', '메모']`
- 한 시트(`주소목록`)에 전체 동의 호수를 이어붙임(flatten).
- 컬럼 폭(`!cols`)도 dashboard와 동일하게 적용.

### 6-3. 호수 → 엑셀 행 매핑 (✅ 확정)
각 호수 1개 = 엑셀 1행. 6개 컬럼 채우는 규칙:

| 컬럼 | 값 | 예시 |
|------|-----|------|
| 도로명 | 입력한 도로명 | 고봉로 |
| 번지 | 입력한 번지 | 725 |
| **건물명** | `{아파트명} {동}` | 백석푸르지오 101동 |
| **새부주소** | JSON의 호수 | 2101 |
| 금지 | 공란 | |
| 메모 | 공란 | |

- **건물명 = 아파트명 + 동** (예: 아파트명 `백석푸르지오`, 동 `101동` → `백석푸르지오 101동`).
- **새부주소 = 호수** (JSON 배열의 각 문자열, 예: `2101`).
  - ※ 컬럼 헤더는 dashboard 코드 그대로 `새부주소` 유지(오타 포함), 값만 호수.
- 도로명 · 번지는 입력값 그대로.

> 스펙 100% 잠금 완료. 남은 미세 결정: **동 이름 입력 포맷**(`101` 입력→`101동` 자동 표기 vs `101동` 직접 입력) — 구현 시 `101동`으로 정규화 처리 예정.

## 7. 작업 순서 (구현 시)
1. `/apart` 라우트 스켈레톤 + `/dashboard` 이동 버튼.
2. 단지 공통 입력(아파트명 · 도로명 · 번지) + 동 일괄 입력 폼(동 이름 + JSON 호수 배열, 최대 ~10개) + JSON 파싱/검증.
3. 동 목록 UI(추가/삭제/편집) + 로컬스토리지 자동 저장/복원.
4. 출력하기 — 전체 호수 flatten → 6컬럼 조립(건물명=`{아파트명} {동}`, 새부주소=호수) → dashboard 고정 형식 xlsx 1개 생성/다운로드.
5. 빌드/린트 검증 (`npm run build`, `npm run lint`).